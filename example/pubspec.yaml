name: whisperr_example
description: Example app for the Whisperr Flutter SDK.
publish_to: none
version: 0.1.0

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter
  whisperr:
    path: ../

dev_dependencies:
  flutter_lints: ^4.0.0
