include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    - example/**
  language:
    strict-casts: true
    strict-raw-types: true

linter:
  rules:
    - prefer_final_locals
    - avoid_print
